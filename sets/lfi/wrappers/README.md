# LFI wrapper atoms

The MVP keeps only read-oriented PHP filter prefixes used for controlled source-disclosure
exercises. PHP's official wrapper documentation is the source of truth.

Write-capable, execution-oriented, remote, archive, and complete wrapper payloads do not belong in
the built-in atom set.
