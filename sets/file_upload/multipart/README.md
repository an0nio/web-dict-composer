# Multipart upload atoms

This directory contains short, semantic multipart field-name sets. Values should represent common
file input names, not arbitrary HTTP parameters, headers, MIME values, or complete requests.

Large parameter lists may be consulted for review, but should not be copied without upload-specific
curation.
