# Upload media-type atoms

This directory contains small media-type sets relevant to upload validation. IANA is the primary
registry; SecLists `web-all-content-types.txt` is useful for broad inspection and reproducible
derivation.

Do not mix extensions, multipart field names, magic bytes, or content payloads into these files.
