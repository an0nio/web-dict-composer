# File upload extension sets

This directory contains extension atoms for filename composition. Handler candidates are grouped
by server stack, while application allowlists and browser-interpreted formats remain separate.

Values such as `.php%00.jpg`, `.jpg.php`, filenames, MIME values, and file contents do not belong
here. SecLists extension wordlists and current server handler configuration are review sources,
not inputs to copy wholesale.
